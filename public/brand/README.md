# Brand assets

Save the official Ace Digital Solutions logo files here with these exact names so the
site picks them up automatically:

| Filename               | Which logo                                              | Used for                          |
| ---------------------- | ------------------------------------------------------- | --------------------------------- |
| `ads-icon.png`         | Compact "ADS" octagon mark only                         | Favicon, compact / mobile header  |
| `ads-badge.png`        | Stacked "ADS" + "ACE DIGITAL SOLUTIONS" octagon badge   | Footer, secondary placements      |
| `ads-horizontal.png`   | Horizontal lockup (octagon + "ACE DIGITAL SOLUTIONS")   | Main desktop header logo          |

Prefer transparent-background PNGs (or SVG if you have vector versions — even better).
If you have SVGs, save them with the same base name (e.g. `ads-horizontal.svg`) and tell me.

Brand navy sampled from the logo: **#1B3A63** (see `src/app/globals.css` design tokens).
